import { SearchstationPipe } from './searchstation.pipe';

describe('SearchstationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchstationPipe();
    expect(pipe).toBeTruthy();
  });
});
